import 'package:flutter/material.dart';

class WriteTimecapsuleDiaryScreen extends StatelessWidget {
  const WriteTimecapsuleDiaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('타임캡슐 일기 작성')),
      body: const Center(child: Text('WriteTimecapsuleDiaryScreen')),
    );
  }
}
